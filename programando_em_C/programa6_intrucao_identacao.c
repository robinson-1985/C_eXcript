#include <stdio.h>

int main() {
    int i = 1;


   /*

   1 - toda variável possui um nome;
   2 - toda variavel possui um tipo;
   3 - toda variável possui um tamanho;
   4 - toda variável possui um valor.


   Temos que ter alguns conceitos em mente sobre variáveis:

   a) é temporária;
   b) quando um programa é fechado ou o pc é desligado, tudo s eperde ao desligar;
   c) valores armazenados na memória são voláteis;
   d) o valor antigo da nossa variável sempre será sobreposto. 

   */ 
    

    printf("%d", i); 

    return 0;

}