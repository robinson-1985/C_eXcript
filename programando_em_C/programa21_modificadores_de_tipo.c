#include <stdio.h>
#include <stdlib.h>

int main() {

    int i = 10;

    short int u;
    printf("%i -> variavel i\n", sizeof(i));
    printf("%i -> variavel u\n", sizeof(u));

    /* 
    
    signed = + e -;
    unsigned = +;
    long = aumentar a capacidade de armazenamento da nossa variável;
    short = quando queremos diminuir o  tamanho de armazenamento.

    */


    return 0;
}